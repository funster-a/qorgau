# DEVLOG — журнал разработки Qorǵau

> Ведёт оркестратор. Новые записи — сверху. Формат: дата-время, что сделано, что дальше.

## 2026-08-08 ~18:30 — ПРОД ЖИВОЙ 🚀

**https://api-production-029f.up.railway.app** — чекер, /demo.html — пульт.

Прод-смоук зелёный: healthz {db:true, llm:groq}; /analyze через Groq
(98/high, degraded=false); bootstrap схемы отработал на чистом
Railway-Postgres; сид 148 сигналов; /admin/* без токена — 401 (ADMIN_TOKEN
в силе, значение в .env.railway); обе страницы 200; privacy/proof работает.

Нюансы прода: DATABASE_URL дополнен `?sslmode=disable` (internal-Postgres
Railway без TLS, lib/pq по умолчанию требует SSL); GROQ_MODEL на проде =
llama-3.1-8b-instant (суточная квота 70b выжжена eval-прогоном, вернуть
завтра при желании). Bot-сервис НЕ задеплоен — ждём валидный
TELEGRAM_BOT_TOKEN (текущий Telegram отвергает 401).

## 2026-08-08 ~18:20 — параллельная разработка завершена, интеграция зелёная

**Фронт (агент):** index.html переписан (SVG-спидометр, RU/QZ ~40 строк, баннер
волн с LIVE, регионы, скелетоны, модалка зон данных), demo.html — пульт
(seed/spike/clear, тумблеры флагов, живой поток, healthz). Всё inline, ноль
внешних запросов. Проверено живьём против API со скриншотами headless Edge.

**Бэк (агент):** api разбит на 7 файлов; промпт — engine/prompt.md (11 few-shot,
грузится с диска, логируется источник); Groq 5с+ретрай; pgcrypto (BYTEA,
PII_ENC_KEY); bootstrap схемы с диска (Railway-ready); z-score детектор 60с
с автозакрытием (проверено вживую: открытие на 26/час при среднем 1.2 →
закрытие); все эндпоинты docs/API.md; rate limit 10/мин burst 20; бот:
/subscribe,/unsubscribe + broadcast-горутина (30с, fail-open, автоотписка 403).

**KPI-2 (testset n=50: 30 скам/20 не-скам, вкл. пограничные OTP/eGov/доставка):**
- эвристика: accuracy 1.00, P/R 1.00 (порог 0.70)
- Groq llama-3.1-8b-instant: accuracy 1.00, P/R 1.00 (порог 0.85)
- llama-3.3-70b: первый прогон 0.70, но ВСЕ промахи = HTTP 429 (TPM 12k,
  TPD 100k выжжен eval'ом); дошедшие до модели кейсы — все верны. Промпт сжат
  14.3KB→10.8KB. Вывод: на демо сегодня — 8b-instant (отдельная квота).
- Ключевая правка эвристики: направление запроса кода («назовите код» = скам
  vs «Код: 1234. Никому не сообщайте» = легитимная СМС, −35).

**Интеграция (оркестратор):** vet/build чисты, полный smoke: healthz JSON,
analyze degraded=false (98, маска +7 7** *** ** 78), flags, seed 132, summary
с регионами, privacy/proof (шифртекст), threat-feed, обе страницы 200.
Docker-образы api и bot собираются.

**Проблемы:**
- ⚠ TELEGRAM_BOT_TOKEN отвергается Telegram (401 на getMe) — нужен новый токен
  от BotFather. Код бота исправен. Деплой bot-сервиса придержан до токена.
- Мелочь: /analyze отдаёт IoC type "url" (контракт: domain) — фронт терпим,
  поправить при случае.

**Дальше:** коммит → railway up api → GROQ_MODEL=8b-instant на Railway →
проверка прода → bot после нового токена.

## 2026-08-08 ~17:45 — Railway-инфраструктура готова (код ещё не задеплоен)

- Пользователь авторизовал Railway CLI (`railway login --browserless`).
- Проект `qorgau` (id 1f7a519a), окружение production. Сервисы: **Postgres**
  (managed), **api**, **bot**.
- Домен api: **https://api-production-029f.up.railway.app**
- Переменные заданы (`--skip-deploys`): api — GROQ_API_KEY, GROQ_MODEL,
  DATABASE_URL (reference на Postgres), ADMIN_TOKEN, BOT_API_KEY, PII_ENC_KEY;
  bot — TELEGRAM_BOT_TOKEN, API_URL, BOT_API_KEY. Сгенерированные секреты
  лежат в `.env.railway` (в git не попадает, добавлен паттерн в .gitignore).
- GitHub-app Railway к аккаунту funster-a не подключён → `--repo` дал
  Unauthorized. Деплой пойдёт через `railway up --service api|bot` из рабочей
  копии (после интеграции кода агентов). Автодеплой из GitHub можно включить
  позже в UI Railway одной кнопкой (Connect repo).
- Секреты в логи/чат не выводились.

**Дальше:** дождаться агентов (бэк: api/bot/db/engine; фронт: web) → интеграция
и e2e локально → коммит → `railway up` обоих сервисов → проверка на проде.

## 2026-08-08 ~17:30 — оркестрация запущена

**Состояние на входе:**
- Сквозной срез работает и запушен (`45bee00` → github.com/funster-a/qorgau).
- Groq-путь проверен вживую с реальным ключом: 100/100, `bank_security`, бренд Kaspi
  распознан, `degraded=false`. Фолбэк тоже проверен (без ключа): 100/100 на типовом скаме.
- Docker: postgres на 55432 (5432/5433 заняты локальными службами!), grafana на 3000,
  датасорс + дашборд 8 панелей поднимаются провижинингом, все панели проверены запросами.
- Ключи в `.env`: GROQ_API_KEY, TELEGRAM_BOT_TOKEN — есть. Бот создан, но новый код бота
  ещё не запускался.

**Известные грабли (не наступать повторно):**
- Порты 5432 И 5433 на этой машине заняты локальными PostgreSQL (16 и 18) → контейнер на 55432.
- Git Bash корёжит кириллицу в inline-JSON у curl → тестовые payload'ы класть в файл
  и слать `--data-binary @file` (файлы лежат в scratchpad).
- `go mod tidy` из Git Bash не видит go.mod → использовать PowerShell + `go -C <dir>`.
- При ошибке Groq тело непустое, но `choices` пуст → уже починено (проверка статуса),
  не регрессить.

**Решения:**
- Контракт API v2 зафиксирован в `docs/API.md` — фронт и бэк пишутся параллельно под него.
- Архитектура и разделение зон — `docs/ARCHITECTURE.md`.
- Бэк: агент (api/, bot/, engine/, db/). Фронт: агент (web/). Деплой+интеграция+git: оркестратор.
- Схему БД применяет сам API при старте (bootstrap) — нужно для Railway, там нет initdb.
- Подписчики бота — в зоне `pii` (chat_id — идентификатор).
- Broadcast: бот поллит /campaigns/active, а не API дёргает бота — бот единственный
  владелец TG-токена.

**Дальше:** агенты пишут код → интеграция → e2e → коммит → Railway.

## 2026-08-08 ~16:15 — сквозной срез (закоммичен)

Подробности в сообщении коммита `45bee00`. Ключевое:
- api: .env-загрузка, защита от паники на ошибках Groq, normalize() ответа LLM
  (clamp score, неизвестная схема → other), маскирование телефона по спеке
  `+7 7** *** ** 78`, домен целиком (не ПДн, главный признак кампании), dedupe IoC.
- api отдаёт web/ статикой — одна ссылка на демо, без CORS.
- bot: HTML-режим с экранированием, /help, /campaigns, риск-бар, индикаторы.
- web: баннер активной кампании (поллинг 30с), безопасный рендер (textContent).
- grafana: полный provisioning.
- seed: реалистичные уровни риска, регионы, not_scam, индикаторы всплеска.
